# Daily Maintenance — 2026-07-21

## Task 1: UI Micro-Improvements

**Colour consistency — GoLivePage.jsx**
The entire "you're live" flow used raw `red-400`/`red-500`/`red-600` (LIVE badge, End Stream
button, camera-permission error icon, the big "Go Live" CTA — 6 spots) instead of the
`victory-danger` token (`#FF4757`), which is already used for the same semantic (danger/end
session) on `TimerPage.jsx`'s end-session button. Migrated all 6 to `victory-danger`.

**Accessibility — 3 icon-only control buttons on TimerPage.jsx missing aria-label**
The pause/resume, skip, and end-session buttons (48px, already correctly sized via
`touch-target`) had no `aria-label`, unlike the equivalent controls already fixed on
`TrainPage.jsx`. Added `aria-label` to all three, matching TrainPage's wording
("Resume"/"Pause", "Skip", "End session").

**Accessibility — 2 more unlabeled icon-only buttons**
`LiveFeedPage.jsx`'s "Go Live" FAB (bare `<Plus />`, no text) and `OnboardingQuiz.jsx`'s back
button (bare `<ChevronLeft />`, no text) had no `aria-label`. Added "Go live" and "Go back"
respectively.

## Task 2: Security Audit

No new findings. Re-read `backend/server.py` (4250 lines, unchanged since 2026-07-20's audit
per `git log`) against all prior logs.

**Checked, already correct — no change needed:** CORS is still an explicit origin allowlist;
all 58 write endpoints checked for auth coverage (all authenticated except the intentionally
public register/login/session-exchange/waitlist/ads-checkout); rate-limit/quota coverage on
every AI-cost endpoint — `/ai/analyze-video` and `/tts/generate` are gated by
`check_and_consume_ai_tokens` (a per-user quota, stronger than a raw rate limit),
`/ai/generate-feedback` does no external API call (pure local computation, no cost to gate);
no hardcoded secrets (grepped for literal key/secret/token/password assignments); no raw
Mongo query interpolation (`search_fighters`'s `$regex` still uses `re.escape`). Noted but not
worth patching: `/onboarding/generate-avatar` and `/emotes/generate` build Pollinations.ai
image URLs without a rate limit while the near-identical `/training-partner/regenerate-avatar`
has one — but the URL is fetched client-side by the browser, not server-side, so there's no
backend cost to abuse; too low-severity to touch.

## Task 3: Debug Sweep

No `TODO`/`FIXME` found. No unhandled promise rejections — every `.then()` chain has a
`.catch()` (including `usePushNotifications.js`'s two-step `.then().then().catch()`, not
explicitly checked on prior days but confirmed fine). Same 8 `console.error` sites as
2026-07-20 (`usePushNotifications.js` x2, `FighterBuddyCreator.jsx` x2, `OnboardingFlow.jsx`
x2, `SessionDetailPage.jsx`, `DrillCard.jsx`) — all previously reviewed as intentionally
decorative/supplementary, no new findings.
