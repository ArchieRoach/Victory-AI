# Daily Maintenance — 2026-07-26 (second pass)

Note: the branch/log dated `2026-07-26` (merged as `0290bcc`) was actually the catch-up run for
the missed `2026-07-25` slot, run late. This is the genuine 2026-07-26 pass, on top of that
already-merged work.

## Task 1: UI Micro-Improvements

**Broken affordance — `TrendingClipsPage.jsx` comment count rendered as a dead `<button>`**
The comment-count control in the clip action row (`<Heart>`/`<MessageCircle>`/`<Share2>` row) had
no `onClick` at all, unlike the identical-looking like/share buttons beside it and the equivalent
comment-toggle buttons in `HomePage.jsx`/`FeedPage.jsx` (which open a comments panel). It still
carried hover styling implying it was clickable. This page has no comments panel/API wired up for
clips, so building one is out of scope for a micro-improvement — instead changed the element from
`<button>` to a non-interactive `<div>` and dropped the hover-color transition, so the UI stops
promising an interaction that doesn't exist.

**Touch target — `HomePage.jsx` "Post comment" send button**
36×36px (`w-9 h-9`), below the 44px+ minimum this same file already uses one button up on the
"Report comment" control (`w-11 h-11 ... touch-target`). Bumped to `w-11 h-11` + `touch-target` to
match its sibling.

**Typography — `HomePage.jsx` "Coach's Corner" label**
Ad-hoc `text-victory-lime text-[10px] font-bold uppercase tracking-[0.15em]` duplicating
`.section-label` (`text-victory-lime text-[10px] font-bold uppercase tracking-[0.18em]`) almost
exactly. Replaced with `section-label` (kept the `mb-1` spacing utility alongside it).

**Checked, not touched:** ran a fresh icon-only-button/aria-label sweep (script-assisted, not just
grep) across `pages/` and non-`ui/` `components/` — all candidates it surfaced already had visible
text labels or an existing `aria-label`. Checked remaining raw `red-400`/`red-500` usage
(`StreamerDashboardPage`, `StreamViewPage`, `HomePage`, `TrainPage`, etc.) — all notification
badges, live-indicator dots, or destructive-hover-action affordances, not the blocking-error-badge
pattern prior passes migrated. Checked `TrainPage.jsx`'s other uppercase-tracking labels
("Ready to work", rest-timer label) — larger/differently-weighted status headlines, not section
labels, left as-is. `CreatePostPage.jsx` has no loading-skeleton gap — it's a pure form with no
initial data fetch.

## Task 2: Security Audit

No commits landed on `main` since this morning's catch-up merge, so this was a fresh read of
`backend/server.py` rather than a re-litigation of already-covered ground.

**Fixed — `/onboarding/generate-avatar` and `/training-partner/regenerate-avatar` had no rate
limit:** Both build a Pollinations.ai image URL from a prompt (one includes user-supplied
`favorite_fighter` free text, already passed through `is_content_flagged`) and are called by an
authenticated user with no throttle at all — unlike every sibling generation endpoint in this file
(`/emotes/generate` → `_rate_limited("emote_generate:...", 10, 60)`, `/cloudinary/signature` →
`_rate_limited("cloudinary_sig:...", 20, 60)`, `/ai/analyze-video` and `/tts/generate` → AI-token
quota checks). Added `_rate_limited(f"avatar_generate:{user_id}", 10, 60)` to both, matching the
emote-generate convention (same shared key namespace since they're both avatar-mutations on the
same training partner).

**Checked, not touched — `/notifications/subscribe` (line 1841):** looks like a legacy sibling of
`/push/subscribe` (fixed this morning), but it upserts a single doc keyed only by `user_id` (not
by endpoint), so it can't grow unboundedly the way `/push/subscribe` could before its fix — no rate
limit needed on correctness grounds. It does appear to be dead code: no reference to
`notifications/subscribe` anywhere in `frontend/src`. Not removing it in an unattended maintenance
pass (could still be called by an older client build) — flagging here for a human to confirm and
delete if genuinely unused.

**Checked, not touched:** CORS is still a single explicit `allow_origins` list; no hardcoded
secrets (Cloudinary/ElevenLabs/Stripe keys all via `os.environ.get`); `/tts/generate`'s `voice_id`
is regex-validated before being interpolated into the ElevenLabs URL path; `generate_round_feedback`
does no external calls or DB writes, so needs no throttle; all 114 `api_router` handlers plus the
one `app`-level Stripe webhook route still match the previously-established public/authenticated
split.

`python3 -m py_compile backend/server.py` passes.

## Task 3: Debug Sweep

No `TODO`/`FIXME` in `frontend/src`. Re-ran the fire-and-forget-promise sweep (script-assisted this
time, not just `grep -A`) across all `.jsx` files for bare `axios`/`fetch` calls with no `await`,
`.then`, or `.catch` — the handful of hits were `Promise.all([...])` array entries (each already
individually `.catch()`-guarded or wrapped by an outer `try`/`catch`) or calls to local `fetch()`
helper functions that already handle their own errors internally, not raw unhandled promises.

Re-checked the four files with bare `console.error`/`console.warn`: `DrillCard.jsx`'s catch already
surfaces an inline "Couldn't load this drill" card state (not silent, just not a toast — correct
for a card-level failure); `SessionDetailPage.jsx`'s `fetchPreviousSession` failure is genuinely
decorative (only feeds an optional comparison overlay on `RadarChart`, page still works without
it); `FighterBuddyCreator.jsx` and `OnboardingFlow.jsx` both already pair their `console.error`
calls with `toast.error`.

No new findings — everything already correct or already fixed by a prior pass.
