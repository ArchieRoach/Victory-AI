# Daily Maintenance — 2026-07-18

## Task 1: UI Micro-Improvements

**Accessibility — unlabeled/invisible interactive controls**
`TrendingClipsPage.jsx` had a completely empty `<button onClick={togglePlay} />` (no icon, no text,
no label at all) used as an invisible tap-to-pause overlay, plus its sibling play button also lacked
an `aria-label`. Fixed both with "Play video"/"Pause video" labels. `PublicProfilePage.jsx`'s clip-grid
button (thumbnail → play) had the same gap — added a dynamic "Play clip"/"Pause clip" label.

**Colour consistency**
`PublicProfilePage.jsx`'s viral badge on the clips grid used raw `bg-orange-500` instead of
`victory-orange` — same pattern already fixed on HomePage and ShareSheet.

## Task 2: Security Audit

**Finding (fixed): `create_clip`'s caption bypassed content moderation.** Unlike `create_post`, which
checks its caption via `is_content_flagged()`, `POST /api/streams/{stream_id}/clip` took a `caption`
query param and stored it directly as a public post caption with no check at all. Added the same
check used everywhere else.

Also reviewed `whip_proxy` — correctly scoped (ownership check, `stream_key` never leaves the server),
no issues.

## Task 3: Debug Sweep

No `TODO`/`FIXME` found. Same 6 `console.error` sites as previous days, all previously reviewed.
