# Daily Maintenance — 2026-07-17

## Task 1: UI Micro-Improvements

**Touch targets + accessibility — icon-only buttons**
Found 4 more in the same vein as prior days: `DiscoverPage.jsx` (clear-search X, no touch target at
all), `TokensPage.jsx` (back button), `PushPrompt.jsx` (dismiss X), `PublicProfilePage.jsx` (a second
back button — the profile header one, distinct from the followers-modal one fixed earlier). All
brought to `w-11 h-11 touch-target` + `aria-label`.

## Task 2: Security Audit

**Finding (fixed): moderation coverage from 2026-07-14 had real gaps.** The original sweep covered
post captions, comments, chat, display name, bio, training-partner name, emote names, and the
`favorite_fighter` avatar-prompt text — but missed several other freeform fields that reach public
surfaces, some of them high-visibility:

- **`/ads/checkout`** (`brand_name`, `tagline`) — becomes the **app-wide sponsor banner shown to every
  user**. This was the single highest-visibility unmoderated surface in the app and had zero checks.
- **`send_tip`** (`message`) — broadcast to every viewer in a stream's live chat via the tip alert.
- **`create_competition`** (`title`, `description`) and **`vote_on_competition`** (`comment`) — public
  competition content and judge comments.
- **`create_stream`** / **`update_stream`** (`title`, `description`) and
  **`create_scheduled_stream`** (`title`, `description`) — shown in the public streams list and
  schedule.

Added `is_content_flagged()` checks to all of the above, same pattern as the rest of the moderation
system (reuses the existing `OPENAI_API_KEY`, fails open on API errors).

## Task 3: Debug Sweep

No `TODO`/`FIXME` found. Same 6 `console.error` sites as previous days, all previously reviewed.
